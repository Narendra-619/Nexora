apiVersion: v1
kind: Service
metadata:
  labels:
    app: nexora-frontend
  name: nexora-frontend
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nexora-frontend
status:
  loadBalancer: {}

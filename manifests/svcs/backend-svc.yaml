apiVersion: v1
kind: Service
metadata:
  labels:
    app: nexora-backend
  name: nexora-backend
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: nexora-backend
status:
  loadBalancer: {}
